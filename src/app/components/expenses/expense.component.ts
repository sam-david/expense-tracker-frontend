export class Expense {
  id: number;
  description: string;
  amount: number;
  transactionDate: Date;

  // template: `<h1>{{title}}</h1><h2>{{hero.name}} details!</h2>`
  template: `<h1>{{title}}</h1><h2>{{hero.name}} details!</h2>`
}
